[package]
name = "arcm-geo"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "26.0.0", features = ["testutils"] }
arcm-types = { path = "../../shared/types" }

[lib]
crate-type = ["cdylib"]
