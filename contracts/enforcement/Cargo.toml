[package]
name = "arcm-enforcement"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "26.0.0"
arcm-types = { path = "../../shared/types" }

[lib]
crate-type = ["cdylib"]

[features]
testutils = ["soroban-sdk/testutils"]
